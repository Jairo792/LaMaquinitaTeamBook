priority_queue<int> pq; 
pq.push(x);// Inserta el elemento x
pq.top();// Devuelve el mayor elemento (maximo)
pq.pop();// Elimina el mayor elemento
pq.empty();// Retorna true si esta vacia
pq.size();// Numero de elementos
//Coordenadas cilindricas entra fija
